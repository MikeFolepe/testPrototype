export type PlayerScore = {
    score: number;
    playerName: string;
    isDefault: boolean;
};

export type PlayerDB = {
    _id: string;
    score: number;
    playerName: string;
    isDefault: boolean;
};
